#!/usr/bin/env python

# supplies = ['pens', 'staples', 'flamethrowers', 'binders']
# for i in range(len(supplies)):
#     print('Index ' + str(i) + ' in supplies is: ' + supplies[i])

cat = ['fat', 'orange', 'loud']
# size = cat[0]
# color = cat[1]
# disposition = cat[2]
size, color, disposition = cat
# size, color, dispositon = 'skinny', 'black', 'quiet'
print(cat)

# multiple assignment
# a = 'AAA'
# b = 'BBB'
# a, b = b, a

# augmented assignment operators
# += 1 # spam = spam + 1
# -= 1 # spam = spam - 1
# *= 1 # spam = spam * 1
# /= 1 # spam = spam / 1
# %= 1 # spam = spam % 1
spam = 42
spam += 1
print(spam)
